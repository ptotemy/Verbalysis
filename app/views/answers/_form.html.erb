<%= form_for @answer do |f| %>


<% if @answer.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@answer.errors.count, "error") %> prohibited this answerfrom being saved:</h2>

          <ul>
            <% @answer.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>



<table class="condensed-table">
      <tr>
        <td><%= f.label :question_id %></td>

        <td><%= f.select :question_id,Question.all.collect{|var|[var.name,var.id]} %></td>

            
      </tr>

      <tr>
        <td><%= f.label :statement %></td>

        <td><%= f.text_field :name %></td>
            
      </tr>

</table>
<br>
<br>

<%= f.submit "Submit" ,:style=>"margin:auto",:class=>"btn primary large"%>

<% end %>